# Figure 2c source data

`figure_02c_source.mat` contains registered neuron coordinates, session indices, the
density kernel, and the cortical atlas mask used by the panel renderer.

Generate the location and density panels with
`workflows/neuron_density/render_figure_2c.py`.
