# Figure-generation environments

`environment.yml` provides the Python environment for the standard figure profile.

`topology.yml` provides the environment for the manifold and topology profile. The
two files pin the Matplotlib versions used by their respective figure workflows.

Create and activate the appropriate environment, then follow the commands in the
root `README.md`. Both profiles write generated files under `outputs/`.
